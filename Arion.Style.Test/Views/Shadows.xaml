<UserControl
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d"
    x:Class="Arion.Theme.Test.Views.Shadows"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Arion.Theme.Test.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label
            Content="Shadow"
            FontSize="32"
            Grid.Column="0"
            Grid.ColumnSpan="5"
            Grid.Row="0"
            HorizontalAlignment="Left"
            Margin="10"
            Padding="0"
            Style="{DynamicResource Lbl}"
            VerticalAlignment="Top"
            Width="Auto" />

        <Button
            Effect="{DynamicResource El1}"
            Grid.Column="0"
            Grid.Row="1"
            Margin="20"
            Style="{DynamicResource BtnOutline}" />
        <Button
            Effect="{DynamicResource El2}"
            Grid.Column="1"
            Grid.Row="1"
            Margin="20"
            Style="{DynamicResource BtnOutline}" />
        <Button
            Effect="{DynamicResource El3}"
            Grid.Column="2"
            Grid.Row="1"
            Margin="20"
            Style="{DynamicResource BtnOutline}" />
        <Button
            Effect="{DynamicResource El4}"
            Grid.Column="3"
            Grid.Row="1"
            Margin="20"
            Style="{DynamicResource BtnOutline}" />
        <Button
            Effect="{DynamicResource El5}"
            Grid.Column="4"
            Grid.Row="1"
            Margin="20"
            Style="{DynamicResource BtnOutline}" />
        <Button
            Effect="{DynamicResource El6}"
            Grid.Column="5"
            Grid.Row="1"
            Margin="20"
            Style="{DynamicResource BtnOutline}" />
    </Grid>
</UserControl>
